# ⚡ Быстрый запуск для просмотра макета

## Шаг 1: Установите Flutter

Если Flutter не установлен:
- Windows: `winget install --id=Google.Flutter`
- Или скачайте с [flutter.dev](https://flutter.dev)

## Шаг 2: Откройте терминал в папке проекта

```bash
cd C:\Users\Нурсултан\Desktop\Cleanapp
```

## Шаг 3: Установите зависимости

```bash
flutter pub get
```

## Шаг 4: Запустите приложение

### Вариант A: В браузере Chrome (самый простой)
```bash
flutter run -d chrome
```

### Вариант B: На Windows Desktop
```bash
flutter run -d windows
```

### Вариант C: На Android эмуляторе
```bash
flutter run
```

## ✅ Готово!

Приложение откроется и вы увидите макет. 

**Примечание**: Без файла `.env` приложение запустится в демо-режиме (без Supabase), но все экраны и UI будут видны!

## 🔄 Если что-то не работает

```bash
flutter clean
flutter pub get
flutter run -d chrome
```

## 📱 Доступные экраны

- Главная (`/`) - карусель акций, быстрый заказ
- Поиск (`/search`) - поиск услуг
- Календарь (`/calendar`) - выбор даты и времени
- Тарифы (`/tariffs`) - список тарифов
- Профиль (`/profile`) - профиль пользователя

Навигация через нижнюю панель!


