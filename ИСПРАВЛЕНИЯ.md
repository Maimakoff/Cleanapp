# ✅ Исправленные ошибки и изменения

## 🔧 Исправленные проблемы

### 1. ✅ Добавлен метод `getBookedDates()` в `SupabaseService`
   - **Файл**: `lib/core/services/supabase_service.dart`
   - **Проблема**: Метод отсутствовал, но использовался в `calendar_screen.dart`
   - **Решение**: Добавлен метод для получения забронированных дат

### 2. ✅ Исправлены импорты
   - **Файл**: `lib/services/auth_service.dart`
     - Изменено: `import 'supabase_service.dart'` → `import 'package:cleanapp/core/services/supabase_service.dart'`
   - **Файл**: `lib/screens/calendar_screen.dart`
     - Изменено: `import '../models/booking.dart'` → `import 'package:cleanapp/core/models/booking.dart'`

### 3. ✅ Исправлен параметр в `AuthService.signUp()`
   - **Файл**: `lib/services/auth_service.dart`
   - **Проблема**: Использовался `metadata` вместо `data`
   - **Решение**: Изменено на `data` для соответствия API `SupabaseService`

### 4. ✅ Удален дубликат `lib/services/supabase_service.dart`
   - **Причина**: Существовал дубликат сервиса, который не использовался
   - **Решение**: Удален, все импорты используют `lib/core/services/supabase_service.dart`

### 5. ✅ Удален дубликат `lib/models/booking.dart`
   - **Причина**: Существовал дубликат модели, который мог вызывать конфликты
   - **Решение**: Удален, все импорты используют `lib/core/models/booking.dart`

### 6. ✅ Исправлен URL в файле `.env`
   - **Было**: `https://ottbrluptyhrnqmegchi.supabase.com`
   - **Стало**: `https://ottbrluptyhrnqmegchi.supabase.co`
   - **Примечание**: Supabase использует домен `.supabase.co`, а не `.com`

## 📋 Проверка кода

✅ **Flutter Analyze**: `No issues found!`
✅ **Линтер**: Нет ошибок
✅ **Импорты**: Все исправлены и унифицированы

## 🚀 Что нужно сделать для запуска

### 1. Установить зависимости
```bash
flutter pub get
```

### 2. Проверить файл `.env`
Убедитесь, что файл `.env` в корне проекта содержит:
```
SUPABASE_URL=https://ottbrluptyhrnqmegchi.supabase.co
SUPABASE_ANON_KEY=ваш_ключ_здесь
```

### 3. Настроить Supabase (если еще не сделано)

#### В Supabase Dashboard:
1. Перейдите в **Settings** → **API**
2. Скопируйте **Project URL** (должен быть в формате `https://xxxxx.supabase.co`)
3. Скопируйте **anon/public key**

#### Настройка Email для тестирования:
1. Перейдите в **Authentication** → **Providers** → **Email**
2. Для тестирования на эмуляторе отключите **"Confirm email"** (или используйте тестовые email)

### 4. Создать таблицы в Supabase

Выполните SQL в Supabase SQL Editor:

```sql
-- Таблица bookings
CREATE TABLE IF NOT EXISTS bookings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES auth.users(id),
  tariff_id TEXT NOT NULL,
  tariff_name TEXT NOT NULL,
  date DATE NOT NULL,
  time TEXT NOT NULL,
  address TEXT NOT NULL,
  phone TEXT NOT NULL,
  area INTEGER,
  total_price INTEGER NOT NULL,
  discount_percentage INTEGER,
  status TEXT DEFAULT 'pending',
  additional_options JSONB,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Таблица referral_bonuses
CREATE TABLE IF NOT EXISTS referral_bonuses (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES auth.users(id),
  discount_percentage INTEGER NOT NULL,
  is_used BOOLEAN DEFAULT FALSE,
  expires_at TIMESTAMP NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Включить Row Level Security (RLS)
ALTER TABLE bookings ENABLE ROW LEVEL SECURITY;
ALTER TABLE referral_bonuses ENABLE ROW LEVEL SECURITY;

-- Политики для bookings (пользователи видят только свои заказы)
CREATE POLICY "Users can view own bookings" ON bookings
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own bookings" ON bookings
  FOR INSERT WITH CHECK (auth.uid() = user_id);

-- Политики для referral_bonuses
CREATE POLICY "Users can view own bonuses" ON referral_bonuses
  FOR SELECT USING (auth.uid() = user_id);
```

### 5. Запустить приложение
```bash
flutter run
```

## ⚠️ Важные замечания

1. **Файл `.env`** должен быть в корне проекта (не в `lib/`)
2. **Не коммитьте `.env`** в Git (он уже в `.gitignore`)
3. **URL Supabase** должен заканчиваться на `.supabase.co`, а не `.com`
4. **Для тестирования** на эмуляторе можно отключить подтверждение email в настройках Supabase

## 📦 Установленные зависимости

Все зависимости уже указаны в `pubspec.yaml`:
- ✅ `supabase_flutter: ^2.5.6`
- ✅ `flutter_dotenv: ^5.1.0`
- ✅ `provider: ^6.1.1`
- ✅ `go_router: ^13.2.0`
- ✅ `http: ^1.2.0`
- ✅ И другие...

## 🎯 Следующие шаги

1. ✅ Все ошибки исправлены
2. ✅ Код проверен (`flutter analyze`)
3. ⏳ Установите зависимости: `flutter pub get`
4. ⏳ Настройте Supabase (таблицы и RLS политики)
5. ⏳ Запустите приложение: `flutter run`

## 📝 Структура проекта (обновлена)

```
lib/
├── core/
│   ├── models/
│   │   └── booking.dart          ✅ Единственная модель Booking
│   ├── services/
│   │   ├── supabase_service.dart ✅ Основной сервис Supabase
│   │   └── booking_service.dart
│   └── config/
│       └── app_config.dart
├── services/
│   └── auth_service.dart         ✅ Использует core/services/supabase_service.dart
└── screens/
    ├── calendar_screen.dart       ✅ Использует core/models/booking.dart
    └── booking_screen.dart        ✅ Использует core/models/booking.dart
```

Все готово к запуску! 🚀
