# 📱 Инструкция по запуску и просмотру макета Cleanapp

## 🚀 Быстрый старт (для просмотра макета)

### Вариант 1: Запуск без Supabase (только для просмотра UI)

Если вы хотите просто посмотреть макет приложения без настройки Supabase:

1. **Установите Flutter** (если еще не установлен):
   - Скачайте с [flutter.dev](https://flutter.dev/docs/get-started/install)
   - Или используйте: `winget install --id=Google.Flutter`

2. **Проверьте установку Flutter**:
   ```bash
   flutter doctor
   ```

3. **Установите зависимости проекта**:
   ```bash
   cd C:\Users\Нурсултан\Desktop\Cleanapp
   flutter pub get
   ```

4. **Временно отключите Supabase** (для демо):
   - Откройте файл `lib/main.dart`
   - Закомментируйте строки с Supabase (строки 13-19)
   - Или создайте пустой файл `.env` в корне проекта

5. **Запустите приложение**:
   ```bash
   flutter run
   ```

   Или выберите устройство:
   ```bash
   flutter devices  # посмотреть доступные устройства
   flutter run -d chrome  # запустить в браузере Chrome
   flutter run -d windows  # запустить на Windows
   ```

### Вариант 2: Запуск с эмулятором/симулятором

#### Для Android:
1. Установите Android Studio
2. Создайте эмулятор через AVD Manager
3. Запустите: `flutter run`

#### Для iOS (только на Mac):
1. Установите Xcode
2. Запустите симулятор: `open -a Simulator`
3. Запустите: `flutter run`

#### Для веб-браузера:
```bash
flutter run -d chrome
```

#### Для Windows Desktop:
```bash
flutter run -d windows
```

## 📋 Полная настройка (с Supabase)

Если хотите протестировать полный функционал:

1. **Создайте файл `.env`** в корне проекта:
   ```
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_ANON_KEY=your-anon-key-here
   ```

2. **Настройте Supabase** (см. SETUP.md)

3. **Запустите приложение**:
   ```bash
   flutter run
   ```

## 🎨 Что вы увидите

После запуска вы увидите:

1. **Главный экран**:
   - Заголовок "Cleanapp"
   - Карусель с акциями (автопрокрутка)
   - Карточки быстрого заказа
   - Кнопка "Срочный Заказ"

2. **Нижняя навигация**:
   - Главная
   - Поиск
   - Календарь
   - Тарифы
   - Профиль

3. **Экран тарифов**:
   - Список всех доступных тарифов
   - Карточки с описанием и ценами
   - Кнопка "Для бизнеса"

4. **Экран календаря**:
   - Календарь для выбора даты
   - Выбор времени
   - Индикация занятых слотов

5. **Экран бронирования**:
   - Детали заказа
   - Форма контактов
   - Промокоды
   - Выбор способа оплаты

## 🔧 Решение проблем

### Ошибка "No devices found"
```bash
flutter doctor
flutter devices
```

### Ошибка с зависимостями
```bash
flutter clean
flutter pub get
```

### Ошибка с .env файлом
Создайте файл `.env` в корне проекта или временно закомментируйте загрузку в `main.dart`

### Приложение не запускается
```bash
flutter clean
flutter pub get
flutter run
```

## 📱 Горячие клавиши во время разработки

- `r` - Hot reload (быстрая перезагрузка)
- `R` - Hot restart (полная перезагрузка)
- `q` - Выход
- `h` - Показать все команды

## 🎯 Рекомендуемый способ для первого запуска

```bash
# 1. Перейдите в папку проекта
cd C:\Users\Нурсултан\Desktop\Cleanapp

# 2. Установите зависимости
flutter pub get

# 3. Запустите в браузере (самый простой способ)
flutter run -d chrome

# Или запустите на Windows
flutter run -d windows
```

## 📸 Скриншоты экранов

После запуска вы сможете увидеть:
- ✅ Главный экран с каруселью
- ✅ Экран поиска услуг
- ✅ Календарь бронирования
- ✅ Список тарифов
- ✅ Детали тарифа
- ✅ Оформление заказа
- ✅ Профиль пользователя

---

**Примечание**: Для полного функционала (авторизация, создание заказов) потребуется настройка Supabase. Но для просмотра макета и UI это не обязательно!


