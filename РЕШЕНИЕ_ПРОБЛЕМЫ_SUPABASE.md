# 🔧 Решение проблемы "Supabase не настроен"

## ✅ Что было исправлено:

1. **Улучшена проверка инициализации Supabase** - теперь используется `Supabase.instance.isInitialized`
2. **Добавлено подробное логирование** - в консоли теперь видно, что именно происходит при загрузке .env
3. **Улучшены сообщения об ошибках** - теперь они более информативные

## 🚨 Если ошибка все еще появляется:

### Шаг 1: Проверьте файл .env

Убедитесь, что файл `.env` находится **в корне проекта** (рядом с `pubspec.yaml`), а не в папке `lib/`.

Файл должен содержать:
```
SUPABASE_URL=https://ottbrluptyhrnqmegchi.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Важно:**
- Нет пробелов вокруг `=`
- Нет кавычек вокруг значений
- Каждая переменная на отдельной строке
- URL должен заканчиваться на `.supabase.co` (не `.com`)

### Шаг 2: Полностью перезапустите приложение

**КРИТИЧНО:** После создания или изменения файла `.env` нужно **полностью перезапустить** приложение:

1. **Остановите** приложение полностью (не просто hot reload)
2. **Закройте** эмулятор/симулятор (если нужно)
3. **Запустите заново**: `flutter run`

**Hot reload (R) НЕ подхватывает изменения в .env файле!**

### Шаг 3: Проверьте консоль при запуске

При запуске приложения в консоли должны появиться сообщения:

✅ **Если все хорошо:**
```
📄 .env file loaded successfully
🔑 Found Supabase credentials in .env
   URL: https://ottbrluptyhrnqmegchi.sup...
   Key length: 208
✅ Supabase initialized successfully!
```

❌ **Если есть проблема:**
```
⚠️ ERROR loading .env file: ...
```
или
```
❌ ERROR initializing Supabase: ...
```

### Шаг 4: Проверьте Supabase проект

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Убедитесь, что проект **активен** (не на паузе)
3. Перейдите в **Settings** → **API**
4. Скопируйте **Project URL** и **anon/public key**
5. Убедитесь, что они совпадают с файлом `.env`

### Шаг 5: Очистите кэш (если нужно)

Если ничего не помогает:

```bash
flutter clean
flutter pub get
flutter run
```

## 📋 Проверка файла .env

Выполните в терминале (в папке проекта):

**Windows PowerShell:**
```powershell
Get-Content .env
```

**Проверка формата:**
```powershell
Get-Content .env | ForEach-Object { 
    if ($_ -match '^([^=]+)=(.*)$') { 
        Write-Host "✅ $($matches[1].Trim()): $($matches[2].Trim().Length) символов" 
    } else { 
        Write-Host "❌ Неправильная строка: $_" 
    } 
}
```

## 🔍 Отладка

Если проблема сохраняется, проверьте логи при запуске:

1. Запустите: `flutter run -v` (verbose режим)
2. Найдите в логах строки с `Supabase` или `.env`
3. Скопируйте ошибки и проверьте их

## ⚠️ Частые ошибки:

1. **"Supabase не инициализирован"** после создания .env
   - **Решение:** Полностью перезапустите приложение (не hot reload)

2. **Файл .env не найден**
   - **Решение:** Убедитесь, что файл в корне проекта, а не в `lib/`

3. **"Invalid URL"**
   - **Решение:** Проверьте, что URL заканчивается на `.supabase.co` (не `.com`)

4. **"Invalid API key"**
   - **Решение:** Скопируйте ключ заново из Supabase Dashboard

## ✅ После исправления:

1. Полностью перезапустите приложение
2. Попробуйте создать учетную запись
3. Проверьте консоль на наличие ошибок

Если проблема все еще есть, проверьте логи в консоли при запуске приложения - там будет подробная информация о том, что именно не так.
